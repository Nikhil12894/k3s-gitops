apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kibana-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/nikhil12894/k3s-gitops.git
    targetRevision: main
    path: manifests/kibana
  destination:
    server: https://kubernetes.default.svc
    namespace: elastic-stack
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
